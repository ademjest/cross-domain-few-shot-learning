在训练我们的officehome时容易早停，即连续多个epochs损失没有下降，9个、10个、12个epoch就停了，验证准确率只有0.33，但是训练完的权重用来测试时，测试准确率只有0.2
我个人感觉是数据量太少了，最少的"Ruler"类别图片只有15张，每个域都只有65类，类别是相同的。
加了数据增强后（在AbstractClassificationDataset.py的load_image里的简单翻转、归一化），出现过拟合现象，训练准确率提高到75%,但是验证准确率仍然33%，epoch能跑到22个才早停，在第5个epoch前验证损失还能保持基本不变，之后就开始快速增大，导致早停。
